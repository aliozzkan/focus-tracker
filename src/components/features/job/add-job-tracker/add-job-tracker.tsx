import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

export function AddJobTracker() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Add Job</CardTitle>
      </CardHeader>
      <CardContent>
        
      </CardContent>
    </Card>
  );
}
